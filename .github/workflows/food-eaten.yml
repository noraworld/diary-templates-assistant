name: '321: 🌿🥗 食事記録（記述）'

on:
  workflow_dispatch:
    inputs:
      food:
        required: true
        description: '食べたもの'
        type: string
      category:
        required: true
        description: '分類'
        type: string
      quantity:
        required: true
        description: '分量'
        type: number
      unit:
        required: true
        description: '単位'
        type: choice
        options:
          - ''
          - '人前'
          - '個'
          - '本'
          - '杯'
          - '袋'
          - '箱'
          - 'g'
          - 'ml'
      score:
        required: true
        description: 'MIND スコア'
        type: choice
        options:
          - ''
          - '+1'
          - '+2'
          - '+3'
          - '+4'
          - '+5'
          - ''
          - '-1'
          - '-3'
          - '-5'
      impression:
        required: false
        description: '感想'
        type: string

jobs:
  food:
    uses: noraworld/diary-templates-assistant/.github/workflows/base.yml@main
    with:
      search_query: "food 食事記録"
      template_name: "food-eaten"
      template_repo: "noraworld/diary-templates"
      version: "0.2.4"
    secrets:
      token: ${{ secrets.GH_TOKEN }}
